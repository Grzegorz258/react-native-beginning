import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    margin: 5,
    padding: 10,
    borderColor: 'grey',
    borderWidth: 2,
    borderRadius: 10,
  },
  todoText: {
    marginLeft: 10,
  },
  deleteIcon: {
    padding: 10,
  },
});

export default styles;
