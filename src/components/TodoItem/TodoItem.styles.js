import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 5,
    padding: 20,
    borderColor: 'grey',
    borderWidth: 2,
  },
});

export default styles;
